/**
 * IDs fijos cuando el usuario completa el paso Mati **sin SDK** (demo sin `identityId`).
 * Solo caracteres `[a-zA-Z0-9_-]` para alinearse con `parseSafeMetamapCallbackId`.
 */
export const DEMO_METAMAP_VERIFICATION_ID = "pp_demo_verification_local";
export const DEMO_METAMAP_IDENTITY_ID = "pp_demo_identity_local";
